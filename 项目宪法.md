# Canteen Smile 项目宪法

## 一、总则

本项目采用前后端分离和后端微服务架构：`web` 为 Vue 应用，`server` 为 Maven 多模块微服务工程。所有实现必须以真实需求、真实接口契约和真实数据模型为依据；AI 与人工开发均不得通过虚构接口、字段或权限数据来“跑通”功能。

## 二、前端宪法

1. 技术栈固定为 Vue 3 + Vite + TypeScript，使用 pnpm 管理依赖，禁止混用 Vue 2。
2. 应用装配置于 `src/app`，业务模块置于 `src/modules`，共享基础设施置于 `src/shared`，目录职责不得混淆。
3. 所有请求必须通过统一 Axios 实例；业务代码禁止直接调用 `axios.create`、原生 `fetch` 或其它请求库。
4. 成功、失败、警告提示必须通过统一反馈服务展示，禁止在业务模块散落组件库静态提示调用。
5. 写操作必须同时具备按钮提交态保护和 Axios 相同并发请求保护，禁止连续点击产生重复请求。
6. 不得猜测后端路径、字段、枚举、错误码或分页结构；未确认契约应显式标记待确认，不得伪造 Mock 充当真实实现。

## 三、后端宪法

1. 使用 Spring Boot、Spring Cloud Gateway、JDK 17、Redis、PostgreSQL、MyBatis；Maven 多模块构建，Jackson 解析 JSON，SLF4J + Logback 记录日志。
2. `server` 必须按微服务拆分，禁止实现为单个 Spring Boot 服务。外部请求统一进入网关；可部署服务以 `smile-<domain>-service` 命名，共享能力以 starter/common 形式提供，公共模块不得承载业务逻辑。
3. 服务按业务能力划界并独立部署、独立配置、独立拥有数据。禁止跨服务直连数据库、共享 Mapper 或共享 Entity；跨服务一致性优先使用最终一致性、幂等和补偿，分布式事务必须经架构评审。
4. 服务间调用必须通过显式 Client 和版本化 DTO 契约，设置连接/读取超时、有限重试、熔断降级与链路追踪；仅对幂等操作重试，禁止循环依赖和级联无界重试。
5. 网关负责路由、统一入口与粗粒度登录认证；业务服务负责接口权限、角色权限、组织权限和数据权限的最终校验，禁止因网关已鉴权而信任任意内部请求。
6. 每个业务服务内部按业务模块分包并遵循 `controller -> service -> mapper` 分层调用。Controller 禁止直接操作数据库、Redis 或第三方服务。
7. 类、方法、枚举、字段及变量使用驼峰命名并具有说明性注释。
8. Entity 仅对应数据库表；DTO 用于接口入参或服务间传递；VO 用于返回前端。禁止 Entity 直接作为接口入参或返回值。
9. 对象转换优先手写 Converter，稳定且字段较多时可用 MapStruct；BeanUtils 仅限字段同名同类型的简单对象，复杂对象禁止大量使用 `copyProperties`。
10. 接口使用统一响应结构和 Spring Validation；全局异常处理器统一转换异常，业务异常必须包含明确错误码。
11. 核心业务表必须包含 `created_by`、`created_time`、`updated_by`、`updated_time`、`is_deleted`。主键策略全项目统一，在业务建模前从 UUID、雪花 ID、PostgreSQL sequence、自增 bigint 中确认一种，禁止模块自行决定。
12. 事务统一放在公开 Service 方法上；本地事务不得跨服务传播，禁止依赖私有方法事务，禁止在长事务中调用第三方接口。
13. Redis Key 必须包含项目、服务、业务和标识命名空间；不得猜测业务 Key。所有非永久 Key 必须设置过期时间；分布式锁释放前必须校验持有者。
14. 权限至少覆盖登录认证、接口权限、数据权限、组织权限和角色权限。后端服务执行最终校验；市级、区县、中心校、学校、食堂等数据范围必须进入 SQL 条件，禁止全量查询后在 Java 中过滤。
15. 每个可部署服务的配置均按 `application.yml`、`application-dev.yml`、`application-test.yml`、`application-prod.yml` 拆分。生产数据库/Redis/管理员密码、短信/对象存储/第三方密钥、私钥等只能通过环境变量、配置中心、Kubernetes Secret 或密钥管理服务提供。
16. 接口统一使用 REST 风格；第三方接口统一封装为 Client。
17. 定时任务统一使用 XXL-JOB，必须避免多实例重复执行，禁止一次性加载全部数据到内存。
18. 文件上传必须校验文件类型、大小、数量、后缀、MIME、文件名和存储路径，不信任客户端元数据；大文件上传、导出和图片处理不得长期占用 Web 请求线程。
19. 避免 N+1、循环访问数据库/Redis/第三方接口、无分页查询、`SELECT *`、超大事务/响应、深分页、无条件模糊查询及在 Java 中代替数据库聚合排序。列表接口必须分页，普通分页最大 `pageSize` 为 100；大数据使用游标或主键/时间联合翻页。高频接口关注 SQL、缓存命中、JVM、线程池、连接池、慢查询、P95 和 P99。
20. 禁止 `new Thread()`、`Executors.newFixedThreadPool()` 和默认 `CompletableFuture.runAsync()`。使用有界、命名、可观测的统一业务线程池，明确核心/最大线程数、队列、拒绝策略、任务超时和异常处理；文件、消息、第三方调用等高负载任务按需隔离。异步任务不得依赖未提交事务。
21. 新增微服务必须先明确领域边界、数据所有权、上下游契约、容量目标、超时降级和故障恢复方案；禁止把一个领域机械拆成多个相互强耦合的服务。
22. PostgreSQL DDL 和 DML 统一放在根目录 `sql`，按服务数据库及 `ddl`、`dml` 分类并进行版本迭代、变更登记和人工执行记录。当前阶段不引入 Flyway；服务资源目录不得维护另一套 SQL。任何已在共享环境执行的脚本禁止修改、覆盖、改名或删除，修正必须新增更高编号脚本。
23. 所有数据库表和每一个字段都必须具有明确的中文用途说明。PostgreSQL DDL 必须通过 `COMMENT ON TABLE` 和 `COMMENT ON COLUMN` 持久化表注释与字段注释；建表或新增字段必须在同一迭代脚本中补齐，不得仅依赖 Java 注释、设计文档或字段名称表达含义。表和字段中文注释覆盖率必须达到 100%，否则不得交付；已执行脚本存在缺口时只能新增更高编号修正脚本。

## 四、AI 编码约束

AI 修改前必须阅读目录及构建文件，确认 Spring Boot、持久层、统一响应、异常、Redis、数据库命名、公共方法及同类实现。遵守最小改动原则；无明确要求不得新增框架、替换公共组件、修改公共响应或字段含义、创建重复工具类、改动无关代码、大范围重构、批量格式化、升级依赖或修改生产配置。

AI 不得伪造数据库字段、接口地址、权限编码、Redis Key、字典值、第三方接口或不存在的公共方法。无法确认时只能基于现有证据做明确标注的推断，并保留 `TODO(待确认)`。

后端验证遵循变更影响范围原则。局部字段、方法参数或方法内部实现调整，在未改变模块边界、公共契约和整体结构时，只需完成受影响模块编译及直接相关测试，不要求每次运行整个后端工程的全量测试。明显结构调整、删除整个类、移除整个模块或存在跨模块影响时，必须扩大验证范围并执行后端全量相关测试。任何变更均不得完全跳过验证。

## 五、明确禁止

- 大量使用 `Map<String, Object>`、`SELECT *`、循环查库、全量数据 Java 分页、金额使用 `BigDecimal`。
- 缓存无过期时间、Redis 锁不校验所有者、捕获异常不处理、`System.out.println`。
- 日志输出密码/Token/密钥，提交生产凭据，修改已发布数据库迁移脚本。
- 定时任务重复执行、默认公共线程池、接口无分页返回大量数据、依赖前端完成权限控制。
- 擅自升级框架/依赖，为单一需求修改大量无关文件，未按变更影响范围完成编译或测试即宣布完成。
- 使用 H2 替代 PostgreSQL 验证全部数据库能力。
